# Kurzübersicht über die Verwendung der JMS-Komponenten

mkfifo test.reader.fifo
mkfifo test.writer.fifo

# Broker starten
java -jar clj-jms-activemq-toolkit-1.99.0-standalone.jar -d &
# Je nach Rechner benötigt der Start unterschiedliche lang.
# FIFO -> JMS
java -jar clj-jms-activemq-fifo-reader-0.1.0-SNAPSHOT-standalone.jar -d &
# JMS -> FIFO
java -jar clj-jms-activemq-fifo-writer-0.1.0-SNAPSHOT-standalone.jar -d &

# In einer Shell
cat test.writer.fifo
# In einer zweiten Shell
echo "abcd" > test.reader.fifo


# Optional bei laufendem Broker/toolkit:
# java -jar clj-jms-debug-tool-0.1.0-SNAPSHOT-standalone.jar
# "Kommandos"
# ls, connect <name>, connected, disconnect <name>, q, quit
# ls - Zeigt Topics im JMS an.
# Der Rest sollte selbsterklärend sein.

